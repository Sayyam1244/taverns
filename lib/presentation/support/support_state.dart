import 'support_initial_params.dart';

class SupportState {

const SupportState();

factory SupportState.initial({required SupportInitialParams initialParams}) => SupportState();

SupportState copyWith() => const SupportState();
}